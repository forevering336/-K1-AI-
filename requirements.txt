# ============================================================
# 焊接缺陷检测系统 —— Python 依赖
# 安装方法:
#   pip3 install -r requirements.txt
# ============================================================

# 核心依赖
numpy>=1.21.0,<2.0.0
opencv-python>=4.5.0

# ONNX Runtime (CPU 版本, x86 调试时使用)
onnxruntime>=1.14.0

# Web 仪表盘
flask>=2.3
pyyaml

# 训练/评估（仅在 PC 端 GPU 训练时需要）
# ultralytics

# ============================================================
# 以下为进迭时空 K1 (RISC-V) 板端专用依赖
# 在开发板上需要额外安装 SpaceMIT 加速器包:
#   pip3 install spacemit_ort  (由进迭时空官方提供)
# ============================================================

# 系统依赖 (非 pip, 需要系统包管理器安装):
#   ffmpeg  (用于 weld_live.py 的摄像头采集)
#   安装方法: sudo apt install ffmpeg
